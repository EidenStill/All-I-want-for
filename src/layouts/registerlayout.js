import Register from '../components/register'; // Import the Field component

function RegisterLayout() {
  return (
    <header className="App-header">   
    
     <Register />
    
    </header>
  );
}

export default RegisterLayout;
